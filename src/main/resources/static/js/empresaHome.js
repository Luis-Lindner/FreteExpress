document.addEventListener('DOMContentLoaded', () => {
    console.log('Página Home da Empresa carregada!');

    // Se no futuro você adicionar modais de notificação ou perfil
    // para a Home da Empresa, a lógica para abri-los e fechá-los viria aqui.
    // Exemplo:
    /*
    const notificationBell = document.querySelector('.company-topbar .notification-bell');
    const notificationsModal = document.getElementById('notificationsModal'); // Adicione este modal no HTML se precisar

    if (notificationBell && notificationsModal) {
        notificationBell.addEventListener('click', () => {
            notificationsModal.style.display = 'flex';
        });
        notificationsModal.querySelector('.close-button').addEventListener('click', () => {
            notificationsModal.style.display = 'none';
        });
        window.addEventListener('click', (event) => {
            if (event.target === notificationsModal) {
                notificationsModal.style.display = 'none';
            }
        });
    }
    */
});